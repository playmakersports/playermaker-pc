const useLogin = () => {
  const doLogin = () => {};
  const doLogout = () => {};
  const isLoggedIn = () => {
    return false;
  };

  return { doLogin, doLogout, isLoggedIn };
};

export default useLogin;
